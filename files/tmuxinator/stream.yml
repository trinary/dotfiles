# ~/.tmuxinator/stream.yml
# you can make as many tabs as you wish...

project_name: Tmuxinator
project_root: ~/code/datastream
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - #empty, will just run plain bash
        - top
  - shell: git pull
